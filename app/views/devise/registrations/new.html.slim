.span6.offset1
  = form_for(User.new, :html => {:class => 'form-horizontal'}, :url => registration_path(resource_name)) do |f|
    .control-group
      = f.label :email, class: 'control-label'
      .controls
        = f.email_field :email

    .control-group
      = f.label :username, class: 'control-label'
      .controls
        = f.text_field :username

    .control-group
      = f.label :password, class: 'control-label'
      .controls
        = f.password_field :password

    .control-group
      = f.label :password_confirmation, class: 'control-label'
      .controls
        = f.password_field :password_confirmation

    .form-actions
      = f.submit class: "btn btn-primary"
      = link_to new_session_path(resource_name), class: "btn btn-link" do
        small= t :sign_in
      = link_to new_password_path(resource_name), class: "btn btn-link" do
        small= t :forgot_password


